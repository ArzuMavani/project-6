<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HomePage</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .scrollbar-hide::-webkit-scrollbar {
            display: none;
        }
        .scrollbar-hide {
            -ms-overflow-style: none;
            scrollbar-width: none;
        }
    </style>
</head>

<body class="bg-gradient-to-br from-black to-gray-900 text-white">
    <aside class="fixed top-0 left-0 h-full w-64 bg-gray-800 bg-opacity-90 p-6 shadow-xl">
        <div class="flex items-center space-x-4 mb-8">
            <img src="https://www.logolynx.com/images/logolynx/b2/b2e088e7e0ecd1a86da5cc240049705a.jpeg" alt="Logo"
                class="w-24 rounded-full border-2 border-amber-400">
        </div>
        <nav>
            <ul class="space-y-4">
                <li><a href="#" class="hover:text-amber-400 block">Home</a></li>
                <li><a href="#" class="hover:text-amber-400 block">TV Shows</a></li>
                <li><a href="/add_movie" class="hover:text-amber-400 block">Movies (Add)</a></li>
            </ul>
        </nav>
        <div class="mt-auto space-y-4">
            <a href="#" class="hover:text-amber-400 block">Search</a>
            <a href="#" class="hover:text-amber-400 block">Kids</a>
        </div>
    </aside>

    <main class="ml-64">
        <section class="p-8">
            <h2 class="text-3xl font-bold mb-6 text-amber-400">Popular on Cinema</h2>
            <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-6">
                <% moviesData.map((movie)=>{ %>
                    <div class="relative overflow-hidden rounded-lg shadow-lg transform transition-transform hover:scale-105">
                        <img src="<%= movie.image %>" alt="Movie" class="w-full h-auto">
                        <div class="p-4 bg-gray-900 bg-opacity-75">
                            <h1 class="text-lg font-bold text-amber-400">
                                <%= movie.movie_name %>
                                <br>
                                <span class="text-sm font-medium text-white"><%= movie.movie_ratings %></span>
                            </h1>
                            <p class="truncate text-sm text-gray-300">
                                <%= movie.movie_disc %>
                            </p>
                        </div>
                        <div
                            class="absolute inset-0 bg-black bg-opacity-60 flex items-center justify-center opacity-0 hover:opacity-100 transition-opacity">
                            <a href="/update_Movie?id=<%= movie._id %>"
                                class="px-4 py-2 bg-amber-400 text-black me-2 font-semibold rounded">Edit</a>
                            <a href="/deleteMovie?id=<%= movie._id %>"
                                class="px-4 py-2 bg-red-600 text-white font-semibold rounded">Delete</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </section>
    </main>
</body>

</html>
